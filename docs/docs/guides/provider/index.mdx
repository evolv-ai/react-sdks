import DocCardList from '@theme/DocCardList';

# Provider

The `<EvolvProvider />` is a top-level component that is responsible for instantiating the Evolv Client.

The Evolv Client retrieves the configuration and variables for your projects and reports data, such as confirmation events, back to Evolv.

Choose your path:

<DocCardList />
